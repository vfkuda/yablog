[package]
name = "blog-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
blog-client = { path = "../blog-client" }
clap = { version = "4", features = ["derive"] }
chrono.workspace = true
tokio.workspace = true
